- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf:
        path: tmux.conf
    ~/.vimrc:
        path: vimrc
    ~/.aliases:
        path: aliases
    ~/.config/i3/config:
        path: i3config
    ~/.zshrc:
        path: zshrc
    ~/.SpaceVim.d/init.toml:
        path: spacevim.toml

- shell:
    - [git submodule update --init --recursive, Installing submodules]
