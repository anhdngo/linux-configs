- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.bash_aliases: aliases
    ~/.aliases: aliases

- shell:
    - [git submodule update --init --recursive, Installing submodules]
